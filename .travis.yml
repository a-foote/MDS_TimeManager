language: python
python:
  - "3.4"
  - "3.5"
#setup and install packages
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install codecov
  - pip install datetime
# command to run tests
script:
  - python mdstm_testsuite.py
#submit code coverage
after_success:
  - codecov